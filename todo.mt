# Todo List

## Style the Sidebar
- Add a transition effect
- Trigger animation on hover
- Ensure the background of the `.active` class spans the full `li` element

## Refactor Header Component
- Delete the existing Header component
- Restructure pages to use two local functions:
  - `Main` function
  - `Header` function